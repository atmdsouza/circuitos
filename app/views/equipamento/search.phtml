<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(["equipamento/index", "Go Back"]); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(["equipamento/new", "Create "]); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Search result</h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
            <th>Id Of Fabricante</th>
            <th>Id Of Modelo</th>
            <th>Nome</th>
            <th>Descricao</th>
            <th>Ativo</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $equipamento): ?>
            <tr>
                <td><?php echo $equipamento->id ?></td>
            <td><?php echo $equipamento->id_fabricante ?></td>
            <td><?php echo $equipamento->id_modelo ?></td>
            <td><?php echo $equipamento->nome ?></td>
            <td><?php echo $equipamento->descricao ?></td>
            <td><?php echo $equipamento->ativo ?></td>

                <td><?php echo $this->tag->linkTo(["equipamento/edit/" . $equipamento->id, "Edit"]); ?></td>
                <td><?php echo $this->tag->linkTo(["equipamento/delete/" . $equipamento->id, "Delete"]); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("equipamento/search", "First") ?></li>
                <li><?php echo $this->tag->linkTo("equipamento/search?page=" . $page->before, "Previous") ?></li>
                <li><?php echo $this->tag->linkTo("equipamento/search?page=" . $page->next, "Next") ?></li>
                <li><?php echo $this->tag->linkTo("equipamento/search?page=" . $page->last, "Last") ?></li>
            </ul>
        </nav>
    </div>
</div>
